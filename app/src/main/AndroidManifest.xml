<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bored.boredom.getbored.boredashell.theboredbutton"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" tools:node="remove"/>


    <application
        android:name="com.moapps.advicebestfriend.di.DIKodein"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Borapp"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.videos.genere.videos.GenereActivity"></activity>
        <activity android:name=".views.videos.VideosActivity" />
        <activity android:name=".views.webview.WebViewActivity" />
        <activity android:name=".views.home.HomeActivity"></activity>
        <activity android:name=".views.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.startapp.sdk.APPLICATION_ID"
            android:value="204473818" />
    </application>

</manifest>